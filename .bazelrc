build --incompatible_enable_cc_toolchain_resolution
build --platforms=//toolchain:arm_none_generic  # Default to building for target.
build -c dbg

test --test_output=errors
test --platforms=@local_config_platform//:host  # Tests are currently only on host.
